# Public Assets

This folder contains static assets that are served directly by Next.js.

## Image Files

Place your headshot image here as `divij-headshot.jpg` (or `.png`, `.webp`, etc.).

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

The image will be automatically optimized by Next.js Image component.

